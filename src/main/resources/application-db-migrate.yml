spring:
  flyway:
    enabled: true
    url: ${DB_URL}
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    baseline-on-migrate: true
    baseline-version: 5
  main:
    web-application-type: none

logging:
  level:
    org.flywaydb.core: off